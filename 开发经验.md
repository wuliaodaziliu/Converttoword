# PDF转Word工具 - 开发经验总结

## 1. PyInstaller 打包关键经验

### 1.1 onefile vs onedir 模式

- **onedir（默认）**：生成 exe + 依赖文件夹，稳定性更好
- **onefile**：单个 exe，但启动时有解压延迟，且 pdf2image 的 poppler 检测在 onefile 模式下更容易出问题
- **结论**：pdf2image + poppler 场景推荐 onedir 模式

### 1.2 spec 文件名与 --name 参数

```bash
pyinstaller build_spec.py --name PDFToWordConverter
```

不加 `--name` 时，输出子目录名等于 spec 文件名（不含扩展名）。spec 文件叫 `build_spec.py`，输出就在 `dist/build_spec/` 下。

### 1.3 打包后路径问题

打包后 `sys._MEIPASS` 是 PyInstaller 的临时解压目录，不是 exe 所在目录。

```python
if getattr(sys, 'frozen', False):
    _base = sys._MEIPASS
else:
    _base = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
```

### 1.4 poppler 必须随包分发

Windows 没有预装 poppler，pdf2image 依赖它进行 PDF 渲染。需要：
1. 下载 [poppler-windows releases](https://github.com/oschwartz10612/poppler-windows/releases)
2. 将 `Library/bin/*.dll` 和相关文件加入打包
3. 设置 `os.environ["POPPLER_PATH"]` 指向解压后的 Library 目录

## 2. Tkinter GUI 经验

### 2.1 后台线程避免 UI 卡死

转换操作（pdf2image 是 CPU 密集型）必须在后台线程执行，主线程只负责 UI 更新：

```python
def _run_convert(self, dpi, callback):
    def thread_target():
        results = convert_multiple(...)
        self.root.after(0, lambda: self._on_complete(results))
    threading.Thread(target=thread_target, daemon=True).start()
```

### 2.2 按钮文字颜色

macOS/Windows 某些 Tkinter 版本下 `fg="white"` 不生效，改用 `fg="black"` 更可靠。

### 2.3 进度回调必须用 root.after

线程内更新 UI 必须通过 `root.after(0, callback)` 回到主线程执行。

## 3. GitHub Actions CI 经验

### 3.1 无法跨平台打包

- macOS 只能打包 macOS exe，Windows exe 必须用 Windows runner
- GitHub Actions `runs-on: windows-latest` 可解决

### 3.2 构建产物上传

- 路径用 glob 模式 `dist/*.exe` 可能失效
- 正确做法：构建后找到 exe 所在目录，打包成 zip 再上传
- 建议用 PowerShell 的 `Compress-Archive` 打包

### 3.3 Node.js 版本警告

Actions 用的 Node.js 20 即将废弃，可能需要设置：
```yaml
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
```

### 3.4 构建失败排查

- `console=True` 临时开启控制台查看启动错误
- 检查 `$LASTEXITCODE` 确保 pyinstaller 退出码为 0
- 列出 `dist/` 目录结构确认产物是否生成

## 4. 坑点汇总

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| exe 黑屏无反应 | poppler 路径未正确设置 | 使用 `sys._MEIPASS` 获取正确路径 |
| dist 下找不到 exe | spec 文件名导致子目录名错误 | 加 `--name` 参数或构建后移动 |
| 进度条卡住不动 | 转换在主线程执行阻塞了 UI | 用 `threading.Thread` 后台执行 |
| 图标不显示 | `icon.ico` 不存在 | 检查文件是否存在，None 则跳过 |
| poppler not found | POPPLER_PATH 设置错误 | 确认打包后路径指向 Library 目录 |

## 5. 快速打包命令

```bash
# 本地调试打包
pyinstaller build_spec.py --clean

# GitHub 触发
git tag v1.0.0 && git push origin main --tags
# 或在 GitHub Actions 页面手动 Run workflow
```

## 6. 关键文件路径

```
项目根目录/
├── build_spec.py        # PyInstaller 配置
├── src/
│   ├── converter.py     # 转换逻辑 + POPPLER_PATH 设置
│   ├── gui.py           # Tkinter UI
│   └── logger.py        # 日志模块
├── poppler/Library/     # Windows 运行时（需手动下载）
├── .github/workflows/build.yml  # CI 构建配置
└── dist/                # 打包输出目录
```

---

*文档生成日期: 2026-05-19*
*项目: PDF转Word工具*
*开发者: 昏昏的小猪仔*